#include "main.h"

/**
 * int mul - multiply two integer
 * @a: first number to be multiply
 * @b: second number to be multiply
 *
 * Return: Always mul success
 */

int mul(int a, int b)
{
	{
		return (a * b);
	}
}
